export function Footer() {
  return (
    <footer className="border-t border-border">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-6 py-8 text-xs text-ink-muted sm:flex-row sm:items-center sm:justify-between">
        <p>Solar Property Exchange — every warehouse is a micro-energy asset.</p>
        <p className="font-mono">v0.1.0 starter</p>
      </div>
    </footer>
  );
}
